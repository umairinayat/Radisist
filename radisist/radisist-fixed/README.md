# Frontend Workspace Wrapper

The actual React application is inside:

`radisist-fixed/`

That means the real project root is:

`/root/fyp/radisist/radisist-fixed/radisist-fixed`

Use that inner folder when running `npm`, `vite`, or editing `src/` files.
